@model IEnumerable<Tienda.Core.Cliente>
@{
    ViewBag.Title = "Index";
}

<h2>Listado de Clientes</h2>
<table class="table table-hover">
    <thead>
        <tr>
            <th>N° Documento</th>
            <th>Nombre</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.NroDocumento</td>
                <td>@item.Nombre</td>
            </tr>
        }
    </tbody>    
</table>
